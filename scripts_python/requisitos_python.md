# Requisitos de Python

Instalar las librerías utilizadas en los scripts de Python con el siguiente comando:

```bash
pip install numpy pandas matplotlib seaborn scipy scikit-image scikit-learn scikit-fuzzy statsmodels openpyxl tqdm tifffile tkinter
